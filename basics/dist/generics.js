"use strict";
{
    // const roolnumber : number [] = [3,6,9];
    // const roolnumber : Array<number> = [3,6,9];
    const roolnumber = [3, 6, 9];
    // const mentors : string [] = ['mr x', 'mr y', 'mr z'];
    // const mentors : Array<string> = ['mr x', 'mr y', 'mr z'];
    const mentors = ['mr x', 'mr y', 'mr z'];
    // const boolarray: boolean[] = [true, false, true];
    // type GenericArray = Array<boolean>, ekhan theke porer line ashche
    // const boolarray: Array<boolean> = [true, false, true];
    const boolarray = [true, false, true];
    const user = [
        {
            name: "Mezba",
            age: 100,
        },
        {
            name: "Jhankar Mahbub",
            age: 110,
        },
    ];
    const add = (x, y) => x + y;
    add(20, 30);
    const human = ['mr x', 'mrs y'];
    const UserWithId = [1234, { name: 'ars', email: 'a@gmail.com' }];
}
